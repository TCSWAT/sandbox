<!DOCTYPE html>
<html>
  <head>
    <title>Moved</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

    <meta name="description" content="Moved App">
    <meta name="author" content="SWAT">

    <meta name="viewport" content="width=device-width, initial-scale=1.0">

  </head>

  <style>
    .alert {
      text-align: center;
      color: red;
    }

    .notice {
      text-align: center;
      color: green;
    }

    .red-text {
      color: red
    }

    .app-title {
      font-size: 3vh;
    }

    @media (min-width: 992px){
      .navbar li {
        margin-left : 2vh;
        margin-right : 2vh;
      }

      .footer {
        bottom: 0;
      }
    }

    .footer {
      position: absolute;
      width: 100%;
      font-size: 1.5vh;
      /* Set the fixed height of the footer here */
      height: 5vh;
      text-align: center;
    }

  </style>

  <body>
    <% if user_signed_in? %>
      <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <%= link_to('Sandbox<span class="red-text">API<span>'.html_safe, root_path, class: "navbar-brand app-title") %>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
              <%= link_to('Services', services_path, class: "nav-link") %>
            </li>
            <li class="nav-item">
              <%= link_to('Profile', user_path(id: current_user.id), class: "nav-link") %>
            </li>
          </ul>


          <div class="navbar-nav">
            <%= link_to('Logout', destroy_user_session_path, method: :delete, class: "nav-link") %>
          </div>



        </div>
      </nav>
    <% else %>
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
    <% end %>

    <%= yield %>
  </body>

  <footer class="footer">
    <div class="container">
      &#169; 2019 Tata Consultancy Services. All Rights Reserved.
    </div>
  </footer>

</html>
